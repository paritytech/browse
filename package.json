{
  "name": "browse",
  "private": true,
  "workspaces": ["app", "packages/*"],
  "scripts": {
    "dev": "bun run --cwd app dev",
    "build": "bun run --cwd app build",
    "deploy": "make -C app deploy"
  },
  "overrides": {
    "polkadot-api": "2.1.4"
  }
}
