---
description: Deploy browse-beta00.dot to a network via the deploy skill (arg: previewnet default, or paseo).
argument-hint: "[network]  # previewnet (default) | paseo"
allowed-tools: Bash, Read, Write, Skill
---

Invoke the `deploy` skill, passing `$ARGUMENTS` as the network (default
`previewnet`) and the domain `browse-beta00.dot`.
